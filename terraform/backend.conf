# =============================================================================
# TERRAFORM ENTERPRISE BACKEND CONFIGURATION
# =============================================================================
# This file configures the remote backend for Terraform Enterprise.
# The workspace name is dynamically set via the TFE_WORKSPACE environment variable.

hostname     = "tfe.pwc.com"
organization = "Digital-Platform"

workspaces {
  name = "$TFE_WORKSPACE"
}



